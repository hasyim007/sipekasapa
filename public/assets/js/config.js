/* ============================================================
   KONFIGURASI — SIPEKA SAPA
   ============================================================
   Frontend dan API sekarang disajikan dari satu domain yang sama
   (Cloudflare Worker + Static Assets), jadi API_URL cukup path
   relatif "/api" — TIDAK perlu diisi manual lagi.
   ============================================================ */

const API_URL = '/api';
